# 发布手册

## 命令记录

```shell
docker run -d --name copilotkit-build node:20-alpine-nginx
docker exec -it copilotkit-build /bin/sh
docker start copilotkit-build
docker stop copilotkit-build

docker cp D:\Work\17-智能体平台\0-代码\CopilotKit\CopilotKit copilotkit-build:/root/CopilotKit
```

## 发布流程

```shell
pnpm install
pnpm changeset
pnpm changeset version 
pnpm run build
pnpm -r publish --no-git-checks --access=public
```